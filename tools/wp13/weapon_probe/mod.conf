name = grug_weapon_probe
title = WP13 weapon-ladder probe (disposable)
description = Logs the one weapon/tool ladder as the engine actually registered it. Staged into a throwaway headless game copy with PROBE=, never shipped.
depends = grug_gear, grug_materials, grug_inventory, grug_visuals, grug_classes
