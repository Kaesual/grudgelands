# WP13 playtest round 2, lane 1 — one weapon ladder

Increment record: [`docs/research/wp13-weapon-ladder.md`](../../../../docs/research/wp13-weapon-ladder.md).
Base: `main` at `9e22b0d`.

| File | What it is |
|---|---|
| `weapon-ladder-sheet.png` | **The thing to look at.** Every weapon and tool of the ladder at 6× — seven families × six materials, plus the below-ladder wood/stone column. Cells marked `default` are vendored minetest_game art shown for comparison; everything else is generated by `tools/wp13/gen_weapon_ladder.py`. |
| `static.sh`, `static.txt` | Parser + SETGLOBAL per changed file and tree-wide, the five plain-5.1 sweeps scoped and tree-wide, the fresh-server audit, generator reproducibility for both art generators, the LICENSE-media row check and a texture-literal existence check. |
| `kats.sh`, `kat-luajit.txt`, `kat-puc51.txt` | The five WP13 fixtures — the new gear catalogue KAT plus the wield transform, character visuals, visuals order and ability right-click ones — in one fixed order, run once under each interpreter. Byte-identical. |
| `headless-boot.log` | One `tools/luanti_headless.sh 180` boot on port 31017 with `tools/wp13/weapon_probe/` staged (disposable, never shipped). `[probe]` lines carry the ladder as the engine actually registered it. |

Reproduce:

```
tools/wp13/evidence/20260915-weapon-ladder/static.sh
tools/wp13/evidence/20260915-weapon-ladder/kats.sh luajit
tools/wp13/evidence/20260915-weapon-ladder/kats.sh tools/bin/lua51
PORT=31017 PROBE=tools/wp13/weapon_probe tools/luanti_headless.sh 180
python3 tools/wp13/gen_weapon_ladder.py --sheet tools/wp13/evidence/20260915-weapon-ladder
```
