# The emergent trees of Kezamba, counted in the BUILT map.
#
# Source: tools/wp13/run_capital.sh <out> kezamba full 531802985935182545
# on main f5583e13, the probe's own read-back dumps kezamba-core.tsv and
# kezamba-avenue.tsv. Every count below is a NODE count in the finished
# world, not a count of authored cells.

core (the composition's 95 x 95 pad, 9025 columns)
	tree_cells	3000
	tree_columns	585
	default:jungleleaves	1292
	default:jungletree	1708

control: the east-avenue dump band x 60..260, z -12..12 (5025 columns),
which lies OUTSIDE the core and carries no plot
	tree_cells	108
	tree_columns	52
	default:jungletree	108
	trunk_columns_per_1000	10.3
	expected_trunk_columns_in_9025	93

# The control band is dumped over a limited y range (the road's deck +-6),
# so it carries TRUNKS and almost no canopy: its cell count is a floor, its
# COLUMN count is the usable figure.
