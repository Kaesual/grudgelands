# Corrected regression and bounded engine evidence

Production bytes remain exactly source checkpoint2bc6f33c. The only executable
follow-up changes are the corrected synthetic R6 fixture and the bounded engine
probe. Full `regressions.lua` now passes all thirteen jobs under LuaJIT. The
baseline diagnostic uses exact4491c996 R6 settlement code and the same corrected
stone contract; it reproduces the former ignore/light-context failure. The fix
fills the synthetic below-surface native host, preserving both original rejection
and noop assertions. No production relaxation.

Engine uses seed4151598227737528026 and the committed probe case coordinates.
Port32752 is a fresh world; port32753 reloads exactly that same isolated scratch
world. Six original case content digests match on reload. Actual positives:
carrot19; pepper2/pumpkin2/grain1; ash376; Cave Cap1; Ember Moss2. The Shattered
sample has zero new plants and supplies no positive acceptance claim. The extra
shallow-sea region contains green1/orange3/pink10/skeleton6 coral nodes. The seven
reef variants and fifteen plant rows are exhaustively covered by the deterministic
real-tail fixture; these few engine samples do not claim that coverage.

Dawnmere contains3267 actual VM-written field-soil nodes. Four held blocks sample28
of them. The first run proves all28 timers activate but has no nearby water, so it
does not prove hydration. The second run explicitly places one water source above
one existing authored soil in this scratch world:12 soils hydrate,16 remain dry,
all28 timers run. This test stimulus is not naturally generated irrigation and
not player permission to modify protected fields. A final same-version reload of
this wet/dry state remains pending when this addendum is committed.

All processes use nice19/chrt idle/ionice3, isolated user/XDG storage and bounded
launcher timeouts. The source checkpoint's full input hashes plus these two
changed-input hashes bind the candidate. Root's integrated final micro pair and
independent focused re-review remain pending; this record is not final acceptance.
