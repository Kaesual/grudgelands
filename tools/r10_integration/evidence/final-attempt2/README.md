# Second integrated Round 10 gate attempt

Frozen source: `2fcda6086cdf7c78c50e0037935b39933dd67e6b`, containing the
independently reviewed display-placement correction `561a9c2b`. All six
capital engine processes now finish without errors, with exact services,
displays, precinct and Alchemy witnesses. Their wrappers still fail because
14 of 18 historical overlay digests differ; four corners match. The complete
raw readbacks are retained, with large TSVs gzip-compressed without timestamps.
`source-files.sha256` binds uncompressed original bytes, while `outputs.sha256`
binds this archive. No geometry difference is waived by the runtime success.

The actual settlement-tail projection reproduced all four new Highcourt
engine digests and the old frozen gate digest. It identified a real unintended
cadence change: moving the inner avenue origin also moves lamp/pier positions
along the full road, including the outer gate. Correction and independent
review remain required. This candidate is **not the accepted final build**.

Both LuaJIT halves passed. The PUC half was intentionally terminated after
this new production finding; its partial output is not parity evidence. The
next compact pair must follow the corrected engine/geometry gate, rather than
start while a production-affecting finding remains open.

Six-start generation did pass in both forward/reverse orders and cold/disk
phases, with canonical digest
`27ab0ba785073e61c6a8f83249f5da08e675cdb1066dbe983347063acddcc96f`.
This evidence is explicitly scoped to the source above. The separate launcher
and version/snapshot records are retained. Main delivery has not occurred.
