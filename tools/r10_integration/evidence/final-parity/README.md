# Accepted final Round 10 interpreter pair

Frozen source: `35432bdf232e88a6d633e861b84571fc3411e404`.
The final runner exited0. Its WP13 LuaJIT pre-step passed; the combined LuaJIT
and plain PUC5.1 processes passed and produced byte-identical80,495-byte output:
`7a45a740503855b654015428ca801ecee1fa3a874b4ae33cb60f5cfba1fe3579`.
All7,888 resolved input paths passed the post-run hash verification.
Input-manifestSHA256:
`c1b4b80db105eaf04349cfc200a1caff5d6f8babed0b037042cb6878233f2716`.

The first two attempts remain separately archived as intentionally interrupted,
not passing parity. This is the accepted pair after both integration fixes.
No full census or exhaustive PUC population was run.

Files above200KiB are deterministic gzip streams. `uncompressed.sha256` binds
original runner outputs; `archived.sha256` binds stored files including the
verification layout and runner completion log. Absolute scratch paths in raw
receipts are preserved intentionally. See the central completion record and
independent final evidence review for the complete delivery disposition.
