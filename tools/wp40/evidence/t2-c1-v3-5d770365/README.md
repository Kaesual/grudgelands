# Historical C1-v3 evidence

This directory is the immutable closeout record for the 2026-08-22 run at
commit `5d770365`. `artifact-manifest.tsv` hashes the 25 fixture bytes as they
existed at that commit; it does **not** describe the live fixture paths after
the integrated 2026-08-24 reacceptance.

The current accepted fixture identity and its explicit provenance limitations
are recorded in the sibling `t2-c1-v3-db9c344/` directory and in T2 contracts
Section 14.10. Do not compare this historical manifest to current working-tree
fixture bytes or use its four payload digests as current C-a2 input.
