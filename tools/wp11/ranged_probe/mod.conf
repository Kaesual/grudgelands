name = grug_ranged_probe
title = Mob-pressure ranged probe (disposable)
description = Proves the Bandit Archer registers, joins the bandit camp's slot roll, sees 16 m and actually shoots. Staged into a throwaway headless game copy with PROBE=, never shipped.
depends = mobs, grug_mobs, grug_core
