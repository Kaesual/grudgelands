name = grug_smelt_probe
title = WP26 in-engine smelt probe (disposable)
description = Places three dual furnaces in a forceloaded block, feeds them, and reports what the node timer actually produced and when. Staged into a throwaway headless game copy with PROBE=, never shipped.
depends = grug_smelting, grug_materials, grug_traders, default
