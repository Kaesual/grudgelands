name = grug_hud_probe
title = HUD bar probe (disposable)
description = Round-4 lane H engine probe: runs the shipped HUD join callbacks against a fake player and prints the element table. Never shipped.
depends = grug_core, grug_abilities, grug_xp, grug_money
