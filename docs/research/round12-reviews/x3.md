# Independent Round 12 X3 review

**Verdict: CHANGES REQUIRED.** Reviewed `wp11-r12-talents` at `5799c524`
against `ca55d4a5` (commits `8478e603`, `5a3e8894`, `5799c524`).
Reviewer: native Astra, independent of implementation. Review was read-only
apart from this report and one scratch diagnostic in `/tmp`. No PUC runtime,
world writes, sync, broad suites, or implementation edits were performed.

Scope and rules: primary AGENTS.md, independent-review checklist in
`docs/process/wp-workflow.md`, `docs/research/luanti-lua.md`, living talent
tables §§2.1–2.6 and relevant §§3–4, combat formulas, and R12-T acceptance in
`docs/research/round12-plan/interface-art-talents.md`. Skills eligibility guard
`eaac9849` and concurrent UI work are outside this candidate's review.

## Findings

### High — H1: Recompense, Word of Ruin and Whitehot bypass accepted-hit settlement

Locations: `mods/PLAYER/grug_abilities/kits.lua:817–821`, `:1097–1109`,
`:543–549`; `mods/CORE/grug_core/combat.lua:1126–1130`, `:1189`.

Recompense adds its absorb unconditionally after calling damage, including
when damage returns zero (dodge/friendly refusal). Word of Ruin interprets
the published damage return as acceptance, although `deal_ability_damage`
returns the amount after a successfully called `punch` even when a mob hook
or PvP/absorb pipeline refuses all actual damage. Whitehot's new `on_crit`
callback runs before `target:punch`, so a refused critical attempt starts its
window and consumes its 120-second ICD. Last Word also starts its window/ICD
before damage acceptance.

Reproduction: dodge a talented Smite and observe a fresh Recompense shield;
cast Word of Ruin against a cancelling mob or fully absorbing PvP opponent
and receive drain healing; force a Fireball crit against a cancelling mob
and observe Whitehot. The independent LuaJIT diagnostic directly reproduced
Recompense producing an absorb when the damage seam returns zero.

Fix: carry the exact attacker/target/action and rolled critical/published
amount through the existing accepted-mob/HP-loss settlement authority; commit
these effects once only from acceptance. A positive published return alone
is insufficient. Preserve Word of Ruin's specified pre-armor amount once an
accepted hit is established.

### High — H2: Turn Aside is unreachable with the real talent model

Locations: `mods/PLAYER/grug_abilities/kits.lua:917–926`;
`mods/PLAYER/grug_classes/talents.lua:634–639`, `:983–991`;
`mods/PLAYER/grug_classes/stats.lua:130–133`.

Turn Aside is registered `window = true`. Its bonus accessor returns zero
until its window starts, but the cast asks that accessor whether it should
start a separate status, and never starts the talent window. Consequently
even rank 3 grants no dodge to any shield target. The behavior fixture hides
this by returning the desired bonus unconditionally from a mock.

If the initial gate alone is fixed, the status still lasts its entire timer
after Power Word: Shield is consumed, and a subsequent untalented shield
refresh does not remove it. If a talent window is added naïvely, self-casts
can also count both the talent bonus and status modifier in stats.

Fix: resolve the ranked magnitude without requiring an already-active window,
attach it to the actual target's named Power Word: Shield contribution, and
end it on that contribution's consumption/replacement/expiry and applicable
lifecycle cleanup. Count it once under the ordinary dodge cap. Test with the
real talent accessor, including casting on another class.

### High — H3: Broadstroke and Recompense wear equipment more than once per action

Locations: `mods/PLAYER/grug_abilities/kits.lua:405–412`, `:817–821`;
`mods/CORE/grug_core/combat.lua:1136`;
`mods/PLAYER/grug_abilities/init.lua:1261`, `:1291`.

Each Broadstroke secondary calls damage without the primary swing's action
identity, causing `deal_ability_damage` to create a fresh identity per target.
The primary already settled its own identity. Recompense similarly uses an
implicit identity for Smite damage and a different `{}` for the absorb.
The repair subscriber deduplicates by identity, so a three-target cleave
wears equipped gear three times; a landed Smite plus Recompense wears twice.

Fix: pass the authoritative primary swing identity into the post-effect and
reuse it for all cleave damage; create one Smite identity and share it with
its accepted Recompense effect. The independent diagnostic confirms that both
secondary-hit calls omit an identity. Test actual repair settlement counts,
including a refused secondary target.

### High — H4: Cinderfall cannot burst on ground, walls or other first contacts

Location: `mods/PLAYER/grug_abilities/kits.lua:1054–1057`.

The decided ability bursts at the first thing the crosshair ray meets, but
the implementation requires `current_enemy_target`, rejecting every node,
friendly object, and other blocking contact. A Mage cannot aim at ground
between enemies; on a hostile object it centers on `target:get_pos()` rather
than the actual first ray intersection.

Fix: consume the shared structured combat ray directly, retain its first
physical intersection, and damage only hostiles in the decided radius about
that point. Test node, object, friendly/blocker, empty-ray and range cases
without reading enemy memory.

### Medium — M1: Brand increases radius instead of keeping the decided 2 m

Locations: `mods/PLAYER/grug_abilities/kits.lua:550–552`, `:623–626`.

`fireball_splash = {2,3,4}` is the per-rank damage addition, but it is also
passed to `get_objects_inside_radius` as the radius. Rank 2 hits at 3 m and
rank 3 at 4 m, versus the fixed 2 m in §2.3. The independent LuaJIT probe
reproduced rank 3 requesting 4 m.

Fix: keep radius 2 and separately assemble the ranked damage plus half spell
power. Assert inside/outside spatial boundaries at every rank.

### Medium — M2: Recompense never pays its decided 6% mana cost

Locations: `mods/PLAYER/grug_abilities/kits.lua:803`;
`mods/PLAYER/grug_abilities/init.lua:155–167`.

Smite remains `{mana_percent = 5}` and the effective-cost seam handles only
Whitehot. A Recompense Priest therefore receives the replacement benefit at
the untalented price. This was also observed in the diagnostic.

Fix: resolve Smite to 6% while Recompense is ranked, before affordability,
and spend that same snapshot once. Check exact rounding with non-divisible
base pools, insufficient mana and untalented 5% behavior.

### Medium — M3: New hostile spell branches ignore timed spell-damage bonuses

Locations: `mods/PLAYER/grug_abilities/kits.lua:625–626`, `:682–684`,
`:1058–1066`, `:1101–1105`.

Brand splash, Rimebite, Cinderfall and Word of Ruin pass raw formulas directly
to damage without `spell_damage_value`. Existing Fireball and Smite use that
helper because the central damage seam applies level scaling, not the timed
spell percentage. With an active spell-damage potion/status these new spells
receive no increase, violating `combat_stats.md` §2's multiplier on every
fully assembled hostile spell formula.

Fix: apply the shared spell multiplier once to each complete raw formula,
with an explicit consistent snapshot point for projectile splash. Verify
that support/shield amounts do not acquire this hostile-damage multiplier.

### Medium — M4: Hold Ground immunity survives respec

Locations: `mods/PLAYER/grug_abilities/kits.lua:1041`;
`mods/PLAYER/grug_classes/talents.lua:1117–1121`;
`mods/CORE/grug_core/movement.lua:350–360`.

Hold Ground writes only the movement aggregator's immunity timer. Respec
clears the talent-window table and refreshes stats/kit, with no call clearing
that immunity. Casting Hold Ground then respeccing leaves root/slow immunity
until its old eight-second deadline. §3.2/§3.7 explicitly require timed
talent windows, including Hold Ground immunity, to clear on respec.

Fix: add bounded ownership-aware lifecycle cleanup for this immunity, while
preserving unrelated Scout/movement state. Test the real movement aggregator
through respec/admin level reset as well as death and leave.

### Medium — M5: Last Light shares the Power Word: Shield contribution name

Locations: `mods/CORE/grug_core/combat.lua:1323–1325`;
`mods/PLAYER/grug_trinkets/init.lua:130`.

The compatibility-shaped `set_absorb` wrapper assigns every caller the source
name `power_word_shield`, but the shipped Last Light trinket also calls it.
A Priest shield therefore replaces Last Light's remaining 120-second absorb,
and a Last Light trigger replaces the Priest's contribution. This undermines
the new independent named-contribution aggregator and would also corrupt
Turn Aside ownership once fixed.

Fix: move every shipped writer to an explicit source identity, including
Last Light, retaining its shared cooldown and maximum lifetime. Test coexistence,
same-source refresh, total cap and earliest-expiry-first consumption.

### High — H5: Submitted probes do not establish the required behavioral gate

Locations: `tools/wp11/x3_behavior_kat.lua:32–42`, `:54–65`, `:154–155`;
`tools/wp11/probe_x3/init.lua:14–28`;
`tools/wp11/x3_contract_kat.lua:15–38`.

The behavior fixture mocks actual talent-window reads, damage settlement,
absorbs, statuses, clock/ICDs and range geometry. It invokes closures directly
instead of the cast/swing dispatcher, so it cannot prove cost/cooldown
exact-once behavior. Fireball's actual projectile closure is not captured or
executed. The native probe checks only registration and function presence;
it never runs an ability or settlement. The contract/model coverage asserts
source-token presence rather than reachable consumer behavior. All logged
checks pass despite the reproducible failures above.

Fix: replace the missing tests with bounded real-module probes of each new
ability/replacement/finisher, acceptance versus cancellation, action-identity
deduplication, real windows/ICDs/caps and lifecycle. Run the specifically
required isolated native X3 behavior/settlement probe, not merely mod load.
Keep the session's no-PUC/no-broad-suite restriction.

## Evidence and preservation notes

- Inspected the actual full production diff and focused fixtures/logs. All
  nine entries in `tools/wp11/evidence/20260920-x3/files.sha256` match the
  reviewed candidate. Parser/static evidence exists; no new incompatible
  Lua syntax was identified. PUC runtime is intentionally absent under the
  session override, not falsely claimed as interpreter-parity evidence.
- Ran exactly one focused LuaJIT diagnostic:
  `luajit /tmp/grug-r12-x3-adversarial.lua /tmp/grug-r12-talents`.
  It retains the submitted fixture and real `kits.lua` closures, instruments
  its existing seams, and reproduces zero-damage Recompense, 5% Recompense
  cost, 4 m rank-3 Brand radius, and omitted Broadstroke action identities.
  It is not claimed as an engine or full-pipeline acceptance test.
- Whitehot's resolved cost now correctly chooses 3% before affordability and
  uses the same `effective_cost` for spending. Fireball's ordinary 6% path
  remains. Its trigger authority is still blocked by H1.
- Bellow's 6/8/10 m hostile-mob loop, Hamstring's ranked 5/6/7-second slow
  and 2/2.5/3-second root, Frostbind's ranked 3/4/5 m radius, and Hearten's
  65% splash formula are present. The mob root ticker already queues its
  slow countdown after the root, so the two calls there are not an overlap
  defect. Ruination remains after accepted primary swing settlement and its
  active cap is 50%; the ordinary crit cap is still 30%.
- Scout and Ironbound/Unbroken implementations were not rewritten. Existing
  model evidence retains Scout and armor checks; global changed-seam behavior
  still needs the stronger tests above before preservation can be certified.
- No approval to integrate is given. Required revisions should receive a
  focused independent re-review, especially the High settlement/identity and
  native behavioral findings.

## Independent corrective re-review — 2026-09-20

**Runtime verdict: CLEAN — zero open implementation findings.** Reviewed
`65aaf78c25651a04106e390982f9787aa26252db` against rejected candidate
`5799c524`, including the complete seven-file production correction diff,
the native scenario implementation, runner, model adaptation and recorded
evidence. The original findings above remain historical; all ten are closed
on these reviewed bytes.

| Original finding | Re-review result |
|---|---|
| H1 accepted settlement | Closed. Exact attacker/object matching records mob acceptance, player acceptance requires actual HP loss, and nested punch state is restored before accepted-only effects. Recompense, Whitehot and drain/Last Word now use this seam. Bonus threat also requires acceptance. Native scenarios cover cancellation, dodge, full absorb, unrelated target callbacks, nested punches and successful effects. |
| H2 Turn Aside | Closed. Rank lookup no longer requires an active window. The modifier belongs to the recipient's named PWS contribution, ends on consumption/expiry/untalented refresh, and is cleared for remote recipients on caster respec/death/leave. Real stat reads count self-shield once and retain the dodge cap and Scout window term. |
| H3 action identity | Closed. Accepted swing context is passed to the post-effect and every Broadstroke secondary reuses it. Smite's accepted callback and drain reuse the damage identity for support. Native repair assertions establish one increment for the multi-result actions and zero on refusal. |
| H4 Cinderfall | Closed. The physical intersection of the shared structured ray owns the burst; hostile, friendly and solid-node contacts work, while absent/out-of-range contacts refuse before payment. Enemy memory is not read. |
| M1 Brand radius | Closed. Rank affects damage; splash radius is fixed at 2 m. The native projectile callback is exercised at all three ranks with 1.99/2.01 m boundary objects. |
| M2 Recompense cost | Closed. The central resolver selects 6% before affordability and the dispatcher spends its single snapshot. Untalented 5%, insufficient mana and repeat cooldown refusal are covered. |
| M3 hostile spell modifier | Closed. The complete new hostile spell formulas use the shared multiplier once; Brand snapshots at launch. Real spell-status tests cover the four branches and support isolation. |
| M4 Hold Ground lifecycle | Closed. Its own window identifies the movement immunity for cleanup; respec/admin reset/death/leave are exercised. No Scout implementation was replaced. |
| M5 Last Light identity | Closed. Last Light now writes `last_light`; PWS and the other production writers use explicit names. Same-name refresh, max-HP cap and earliest-expiry consumption are checked. The implicit `set_absorb` wrapper is removed. |
| H5 behavioral evidence | Closed within the documented bounded native-probe scope. The old token/closure fixtures were removed. The new probe retains real loaded consumers, dispatchers, movement, stats, repair and native projectile entity lifecycle, with synthetic player/mob/clock/raycast boundaries explicitly disclosed. |

Verification in this re-review:

- Confirmed clean candidate HEAD and all twelve correction-manifest hashes.
- Read all 531 lines of the native scenario, its entry point and the runner;
  verified recorded `WP11X3 RESULT PASS checks=110` and native LuaJIT identity.
  Reviewed parser/SETGLOBAL/five-sweep and supplementary model evidence.
- Checked the production accepted-mob boundary in
  `mods/ENTITIES/grug_mobs/init.lua:270–288` and the vendored punch sequence
  in `mods/ENTITIES/mobs/api.lua:3216–3262`: custom/CMI cancellation precedes
  the accepted-hit call. Checked the primary reference engine's
  `reference_projects/luanti/builtin/game/register.lua:546–564` against the
  probe's HP-change adapter.
- `git diff --check 5799c524..65aaf78c` passes. No additional runtime suite,
  native server, PUC run, world mutation or production edit was performed.

One **evidence wording correction**, not an implementation defect: the
correction README says opaque table identities were previously ignored by
repair. Lua's original `type(action_id) == "table" and action_id.id or
action_id` already falls back to the table when `.id` is nil. The parenthesized
replacement is behavior-equivalent for these inputs. The actual H3 fix is
sharing one identity across the action's results. The coordinator should
correct this historical explanation when integrating; the new executable
repair-count assertions are still valid evidence.

Integration boundary: this verdict covers the hash above, not the
coordinator's later merge. Preserve Skills' `is_unlocked` execution guards
when replaying `init.lua`, and adapt remaining offline fixtures that called
the removed `set_absorb` API. The recorded tests do not claim network-client
or GUI coverage, and do not claim fallback-interpreter parity under the
session's no-PUC override.

User runtime plan: after integration, exercise each new skill and replacement
on mobs and PvP targets, including declined/dodged/fully absorbed hits; observe
one durability payment per cleave or damage/support action; test Turn Aside
with another player and consuming/recasting/expiring its shield; verify
Cinderfall on ground and objects, Brand's tight splash, Whitehot's visible
cost/window restoration, and Hold Ground cleanup after respec. Recheck Scout
shots/dodge and Unbroken in the same fresh-world session.

## Integrated seam recheck — 2026-09-20

**Verdict: CLEAN — zero open integration-seam findings.** Checked primary
HEAD `1a4b5c5863ba179169ed98a2fe9becfa65fab288` plus the reviewed uncommitted
fixture adaptations against worker `65aaf78c25651a04106e390982f9787aa26252db`.

- The six X3 production files other than `grug_abilities/init.lua` are
  byte-identical to the independently approved worker. In the merged ability
  file, both cast dispatch and selected-swing acquisition retain authoritative
  `is_unlocked` guards. The predicate checks class and actual talent rank;
  normalization removes stale/duplicate/disallowed stacks without re-granting
  missing representations, and equipment removals notify the equipment seam.
  X3's Whitehot/Recompense resolved costs and shared post-swing action context
  are retained alongside the Skills changes.
- `tools/wp39/combat_integration_test.lua` replaces only the removed absorb
  writer API with explicit `power_word_shield` contribution names and adds
  neutral untalented registry/window dependency stubs. Existing assertions and
  amounts are unchanged. The two UI/layout fixtures add neutral absorb-reader
  and modifier-cleanup dependencies; they do not replace or weaken assertions.
  The catalog probe disables unrelated startup map preload only.
- All entries of the final `source-inputs.sha256` match the inspected working
  tree. Recorded focused combat, Skills, layout and talent-UI checks pass.
  The now-completed integrated native logs report `WP11X3 RESULT PASS
  checks=110` and `ROUND12 RESULT PASS routes=596 starters=63 foods=57 armor=4`.
  These are inspected coordinator runs; the reviewer reran no suites.
- The evidence README now correctly describes the behavior-equivalent repair
  parenthesization and identifies shared action identities as the actual fix.

This bounded check approves the X3/Skills integration seam and fixture
adaptations, not unrelated concurrent documentation or other lanes' full
implementations. The GUI runtime plan and explicitly documented synthetic
native-test boundaries above remain applicable.
