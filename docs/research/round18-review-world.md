# Round 18 C/D/G independent review

Reviewer: GPT-6 Astra (native); independent of all reviewed implementation and integration edits. Snapshot: `/tmp/grug-r18-review-world`, `fbdaac67`; baseline `7460c49d`. Review scope: C population/quest policy, D pursuit/vendor settlement, G preparation scheduler, their living documentation, fixtures and the root native population probe. Elapsed: unknown. Fix rounds: 0.

Verdict: CHANGES REQUIRED. Critical: 0; High: 1; Medium: 2; Low: 0. The pending integrated final micro-KAT/native gates are not themselves counted as defects.

## High

1. **Evade reacquisition restarts the return timeout indefinitely.** `mods/ENTITIES/grug_mobs/aggro.lua:24` seeds `grug_damage_at` even while `temp.grug_evading` is active, and `:365` expires that clock by invoking `leash_reset` again. Native `do_attack` calls the new seed hook (`mods/ENTITIES/mobs/api.lua:292`) for both group alerts and NPC acquisition; the existing player-only acquisition veto does not block those routes. `evade_tick` stops the acquired target but does not clear the new clock. Concrete scenario: a returning ambient monster is blocked by terrain with an eligible guard still nearby. Repeated NPC acquisition causes a new reset every roughly 16 seconds, overwriting `grug_evading.started` before the 40-second fallback can fire. The monster remains untouchable indefinitely. A bounded LuaJIT reproduction loading production aggro completed 60 simulated seconds with `grug_evading.started=48` rather than the original 0. Prevent pursuit-clock creation/timeout processing during an active return, and add a regression covering repeated NPC/group acquisition while the return is blocked. Preserve normal reacquisition after arrival.

## Medium

2. **Whitebridge Shire loses every boar variant.** `mods/ENTITIES/grug_mobs/spawn_policy.lua:201` omits `elandor_whitebridge_shire` from the closed boar-selection table, so `:722` rejects base, Plague and Jungle Boar throughout that zone. Whitebridge still explicitly has `settled=true` (`:51`), and its meadow/forest/mud hosts remain in the base boar spawn row. Its decided palette remains settled/forest (world_zones.md:794). This removes the existing neutral boar population and its meat/leather source rather than selecting one local tint. Production-policy LuaJIT reproduction at Whitebridge L25/day returns false for all three identities. Add the appropriate base identity and update the living table/report's misleading eight-base-zone/twelve-total-zone count; cover every settled palette, including this higher-level one.

3. **The prepared native population probe cannot see species minima and fails before producing its 18-point evidence.** `tools/r18_final/probe.lua:52` requires `core.registered_entities[name]._grug_min_level`, but the vendored `register_mob` builds a field whitelist (`mods/ENTITIES/mobs/api.lua:4044`) without that custom field. `register_level_cfg` stores the authored minimum in its private table (`mods/ENTITIES/grug_mobs/levels.lua:364`), and the adapter publishes only pursuit/disposition metadata on the prototype (`init.lua:803`). Thus ordinary entities fail this filter, `allowed` remains zero, and `:59` fails at the first point. Use real captured registration metadata or an existing level authority API instead of nonexistent prototype fields. While correcting this bounded probe, ensure its population claims use the registered spawn-row host intersection and species-specific predicate: `spawn_policy_allows` alone does not include the Husk's Sunscar L7 check or host eligibility. Merely removing the bad filter would let critters or ineligible-host species satisfy the nonempty-roster assertion and still not prove the requested final eligibility boundary.

## Reviewed behavior and evidence

- D classification respects normal/elite versus NPC, passive/critter, rare and boss registration, with runtime camp/rare/boss/retinue/patrol/bespoke exclusions. Ordinary Zombie/Husk/Goblin opt-in is the explicitly approved policy. Damage refresh occurs after actual health subtraction, cancellation, immunity, rounding and friendly-fire decisions. The new guard seam changes only a runtime timestamp; it creates no player tap, threat, XP or loot. Distance and LOS cancellation bypasses share the same ambient predicate. No additional distant scan or terrain load was added. The return-reacquisition issue above is the material lifecycle defect found.
- G changes are limited to scheduler pacing and bounded scan batches. Selection/source/planner output remains untouched. Callback dispatch stays outside native completion callbacks; ordered persistence and retry paths remain. Inspected the stored LuaJIT/static results and two-boot bounded native stop/resume log: one successful tile then cancellation on each boot, resume at cursor 1, immutable mode preserved. Recorded source SHA256s exactly match the reviewed scheduler and fixture. No new preparation run or ETA/world observation was executed. No G implementation defect found.
- C quest changes retain counts/rewards and use Tapir for the early Troll target while leaving later Raincall Lynx available at natural L10. Orc early quest retains Boar below Husk eligibility. Existing registered drops support these substitutions. The author fixture mocks zone queries and does not prove live host intersections; the root probe is a useful intended complement once corrected, not yet evidence of those boundaries. The Whitebridge omission demonstrates why the palette audit must include non-start zones too.
- Reviewed `wp-workflow.md`'s code-review checklist and `luanti-lua.md`, lane reports, production diff and relevant living specs/VENDOR. The preparation hashes and stored static evidence were inspected rather than repeated. No PUC runtime or suite rerun occurred. One small targeted LuaJIT script (`/tmp/r18-review-repro.lua`) verified the two concrete production defects; it does not claim engine-native validation.

Final acceptance still requires the coordinator's corrected final-byte parity/native evidence and focused re-review of these findings. User GUI/runtime acceptance remains separate.

## Focused re-review — fix round 1

Reviewed frozen snapshot `36be14cb` and successful native evidence `/tmp/grug-r18-integration-946xbnpq`. Reviewer remains independent native GPT-6 Astra; elapsed unknown. **All three findings are resolved. Open Critical: 0; High: 0; Medium: 0; Low: 0. C/D/G implementation review PASS, conditional on the coordinator's final combined frozen-byte PUC/LuaJIT evidence.**

- Finding 1: both initial clock seeding and ambient timeout processing now refuse active evade. This preserves the original return deadline even when native `do_attack` reacquires a target between leash passes. The extended compact fixture repeatedly invokes native `do_attack` during a blocked return, asserts the unchanged original timestamp/no pursuit clock, and asserts fallback after 40 seconds. Arrival still clears evade before subsequent normal pursuit can start. The correction addresses both paths that caused the reproduced loop.
- Finding 2: Whitebridge explicitly selects base Boar; the focused fixture checks base acceptance and both variant refusals at L25. Living documentation and the population report now include Whitebridge and the corrected nine-base/thirteen-total zone counts.
- Finding 3: the native runner captures actual `register_spawn_role` inputs in its disposable game copy. The probe now joins those roles to actual registered ABM host lists, derives the local top from the current constructor catalog, and invokes real `mobs:spawn_abm_check`, including natural minimum, clock, domain and species-specific gates. It excludes critters and moves clock changes into the first server callback, verifying the changed engine clock. This is sufficient bounded registration/authority evidence for the requested integration boundary; it does not claim generated terrain density, lighting, clearance, active-object caps or a world-population campaign.

Inspected the successful native log: 18 points cover six cultures at actual levels 3, 6 and 10, with explicit zone IDs and hosts and nonempty day/night combat rosters at every logged point. Day/night counts differ where expected. The final integration marker passes; there is no ERROR or disallowed script-init clock warning. This evidence is from the corrected server-callback run only; the earlier rejected initialization attempt is not acceptance evidence. The samples cross named start/home boundaries where the real authority does so; their logged zone IDs are retained rather than mislabeled as all belonging to the start zone.

Verified production-manifest hashes against the reviewed snapshot and executed-manifest hashes against the actual scratch copy for aggro, policy, adapter, vendored API and scheduler. The two scratch modifications are exactly preparation suppression and spawn-role capture. The executed probe bytes equal the reviewed probe. G production is unchanged and its previously inspected bounded stop/resume evidence remains applicable. No Lua/native suites, PUC process, preparation run or ETA observation were repeated by this reviewer.

Remaining gate: inspect/record the coordinator's final combined compact PUC/LuaJIT parity on the final integrated Lua bytes after the independently reviewed shovel correction. That separate pending gate does not reopen the resolved C/D/G findings. User GUI/runtime acceptance remains separate.

## Final evidence acceptance

Accepted final artifact gate for frozen production commit `4d7adb555494e130feffdde2f83da3d6be4dd05a`. Independent native GPT-6 Astra inspected `tools/r18_final/evidence` without rerunning any test. The two 11-fixture outputs are byte-identical and independently hash to `d82a6c14aae29608178f351fc99822a417d0f414082809e34f40414785e8c2e8`; both recorded exits are zero, stderr files empty, recorded durations PUC 0.067794 s and LuaJIT 0.026646 s. All 55 source-manifest hashes match the current frozen files; all 2,083 production-snapshot hashes match. The executed snapshot differs only by the two previously reviewed scratch instrumentations. Static evidence contains parser PASS for 55 Lua files, expected global writes, and reviewed comments/string/vendor-only sweep hits.

The final native log records the same 18 successful real-authority population points, the additional engine tool-matrix PASS, and integration PASS without ERROR or disallowed initialization calls. Reviewed C/D/G production corrections are unchanged from `36be14cb`. The separate J correction has its own independent review and is not self-certified here.

**Final C/D/G review and evidence verdict: PASS. The previously conditional artifact requirement is closed. Open Critical/High/Medium/Low: 0/0/0/0.** User GUI acceptance and a real fallback-engine runtime test remain separate from this technical review.
