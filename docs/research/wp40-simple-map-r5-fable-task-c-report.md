All bounded reading and targeted verification is complete. Both L1 and L2 are independently confirmed at their exact sources (`map_adapter.lua:843-862` pcall-wraps every VM call including the setters, and `map_adapter.lua:1395-1406` validates the `light_final` buffer after `calc_lighting`; the implementation's light order at `map_adapter.lua:1264-1326` is box context → `get_light_data` → seed-run construction). The foundation question is now resolvable from the frozen sources. Final review below.

---

# Fable Task C — Bounded Completion Pass, WP40 simple-map R5 (commit `212f822d3bef8e87a48958ee2286adf00e525e87`)

## 1. Verdict: ACCEPT

Every file and line range required by this bounded pass was read completely (coverage table in Section 4). Together with the first pass — whose claims I spot-verified against the code rather than trusted — the combined coverage now includes 100% of the R5 semantic core, 100% of the R5 Validator, all three legacy writers, and all eight frozen direct dependencies. I found **no new finding at any severity**, and both prior Low findings are confirmed as documentation-level residuals that block nothing.

## 2. Severity counts

**0 Critical / 0 High / 0 Medium / 2 Low** (the two Lows are the confirmed prior findings L1 and L2; no new finding was added).

## 3. Findings and disposition of prior L1/L2

**L1 — Confirmed as stated, Low, contract-wording defect, not a runtime defect.** The contract claims "All fatal validation precedes step 9" (`docs/research/wp40-simple-map-r5-contract.md:2317`) and "After this point no R5 validation may fail" (`:2500-2501`), but the adapter pcall-wraps every VoxelManip call and raises `fail_vm_contract` on failure at any point (`mods/MAPGEN/grug_mapgen/wp40/map_adapter.lua:843-862`), including `set_data` (`:1364`), `set_param2_data` (`:1367`), the light setters (`:1374-1394`), and the post-`calc_lighting` `get_light_data` buffer-identity and byte-range checks (`:1395-1406`). Reachable only through a *non-conforming* VM (a method that throws, or a light buffer with out-of-range bytes); on a conforming engine these paths cannot fire, all fixtures use conforming proxies, and the pinned engine fact (§2.3, callback error cancels generation) means no partial settlement could persist even if they did. The defensive checks themselves are the right engineering; the contract's blanket wording is over-strong. Smallest safe correction: scope "fatal validation" to R5 *semantic* validation in the R6 contract or the durable R5 review record. The frozen R5 contract is an artifact-bound input and must not be edited; no code, artifact, log, or hash is invalidated, and no re-review of runtime bytes is needed.

**L2 — Confirmed as stated, Low, contract-internal wording inconsistency.** §11.2 step 7 (`:2304-2306`) orders "construct and validate the complete light box/seed-run plan from Section 12.3, **then** call `get_light_data`," but §12.3's own step order retains `light_original` at step 3 (`:2484`) and only then classifies seed candidates at step 4 using "its original packed light byte is exactly 15" (`:2487-2493`) — data that exists only after the read. The implementation follows §12.3's coherent internal order: box construction and context validation (`map_adapter.lua:1264-1285`), then `get_light_data` into `light_original` with full byte validation (`:1286-1297`), then seed-run construction (`:1298-1326`). No runtime effect; the risk is a future implementer enforcing §11.2's literal order, which is impossible to satisfy. Same disposition channel as L1; nothing invalidated.

Neither finding blocks integration or invalidates the immutable R5 artifact or its evidence chain.

## 4. Exact bounded coverage table

| Required item | Status |
|---|---|
| `SNAPSHOT-MANIFEST.txt`; `external-evidence/fable-task-c-pass1.md` | Read completely |
| `docs/process/wp-workflow.md` (independent-review checklist) | Read completely |
| `tools/wp40/simple_map_r5_validate.lua` lines **1040–2624** | Read completely (as 1040–2039, 2040–2624) |
| `tools/wp40/simple_map_r5_validate.lua` lines **3260–5546** | Read completely (as 3260–4259, 4260–5259, 5260–5546) |
| `tools/wp40/simple_map_r5_validate.lua` lines **5838–6826** (end of file) | Read completely |
| `mods/MAPGEN/grug_mapgen/ocean_mask.lua` (747 lines) | Read completely |
| `mods/MAPGEN/grug_mapgen/ocean_mask_mapgen.lua` (632 lines) | Read completely |
| `mods/MAPGEN/grug_mapgen/structures.lua` (892 lines) | Read completely |
| `wp40/simple_map.lua` (2,195 lines) | Read completely |
| `wp40/height.lua` (3,457 lines) | Read completely |
| `wp40/source/simple_map.lua` (926 lines) | Read completely |
| `wp40/schemas.lua`, `wp40/canonical.lua`, `wp40/deterministic.lua`, `wp40/validation.lua`, `wp40/seed_corpus.lua` (19 / 201 / 303 / 365 / 131 lines) | Read completely |
| Targeted verification (items 4–5) | Contract §8.3–8.5 (`:1545-1590`), §11.2 (`:2270-2324`), §12.3 (`:2480-2509`); `planner.lua:265-292, 1270-1329`; `map_adapter.lua:830-874, 1255-1414`; `zones.lua:860-990, 1380-1450` |

No listed file or range remains unread; Grep was used only for locating targeted-verification sites, never as a substitute for a complete read of items 1–3.

## 5. Validator / source / legacy-writer invariant summary

**Validator (previously unread ~4,860 lines).** Every prior-pass claim about these regions is confirmed, and nothing contradicts the accepted evidence. Specifically: the fixture content contracts are independently authored with their own CID/property tables and call observers (`:1000-1107`); fixture plans built by `rewrite_plan` are constrained to emitted opcodes and the closed opcode/policy relation and re-pass `validate_plan` (`:1391-1434`), so no fixture can smuggle a reserved opcode into an apply. The apply matrices assert exact VM call traces including light boxes and per-row seed runs (`assert_exact_vm_calls`, `:1597-1620`). The independent owner oracle re-derives outcome codes, central content/param2, the complete read-only halo (content, param2, *and* light), the exact transaction trace, sunlight seeding including ignore-overtop semantics, the six-face liquid rule, and a full BFS light propagation with a complete light-buffer digest (`:4408-4731`) — a genuinely independent lighting model, not a planner mirror. The seed-zero corpus enforces an exactly-once full-map scan with a five-row ring buffer that fails on any window escape (`:3281-3287, 3434-3666`), materializes and byte-compares production plans at every witness group (≤92), and closes the causeway census with `eligible == resolved` per named interface (`:3676-3690`); the fixed roofed-bridge (−1916/−2071, run {40,43, P3, BRIDGE_CLEAR, AIR, CUT_NATURAL, poi_spur_025}) and equal-surface bank (−456/−1490, hydro_whitebridge_ford) witnesses are pinned exactly (`:3719-3750`). The native-heightmap corpus covers all valid/invalid classes (NaN, ±inf, fraction, out-of-domain, hole, extra key, metatable) with plan-byte invariance (`:3877-4018`); the conflict corpus covers all emitted opcode pairs under both permutations, endpoint splitting, the hidden same-priority triple, the BRIDGE_CLEAR dual-policy conflict, and exactly 6+3 = 9 reserved-opcode/priority rejections feeding the `zero_p7_p8_p9` proof (`:4038-4165, 5024-5032`); the owner-order corpora prove plan-byte order independence and committed-halo insensitivity in both axes with complete halo scans (`:4218-4868`); the JSON parsers are strict (no escapes, no duplicate keys, no trailing bytes, canonical decimals; `:5207-5346`); the dungeon oracle enforces the exact 34-record/31-event population with box algebra and a union (max y −177) strictly below the authored floor (`:5369-5544`); and the micro-KAT builder enforces exact compactness budgets (60 applies, 48 VM fixtures), allocator and plan provenance rejections, and complete family/receipt coverage with self-validation (`:5838-6826`).

**Frozen dependencies.** All eight are hook-free, write-free, engine-free modules whose semantics R5 consumes without reinterpretation: the horizontal evaluator's layout, warp, and difficulty lattices are derived from a fixed-seed layout hash (seed-independent geometry, `simple_map.lua:1218-1341`); the height model's functional-kind vocabulary (`land_grade`, `anchor_platform`, `bridge_deck`, `ford`, `causeway`, `tunnel_floor`), transition tuples (upper/lower/progress/face-mask), contact-face records, and hard-protection volumes match exactly what the R5 planner source passes through and the R5 contract §7 vocabulary requires (`height.lua:3249-3313, 2404-2424`); `canonical`/`deterministic` are strict, safe-integer, injection-based primitives; `validation.lua`'s IPC seam is passive (single-use publish/consume; a callback registration only via an explicitly supplied registrar, never invoked by R5's disabled configuration); `seed_corpus.lua` is a fixed 27-seed roster with a self-verifying KAT.

**Legacy writers / no-double-writer boundary.** All three files are now semantically read, not just hash-pinned. `ocean_mask.lua` (main env: geometry, IPC, mapgen-script registration, healing LBM), `ocean_mask_mapgen.lua` (mapgen env: carve, re-dress, shell clean, sunlight stamp), and `structures.lua` (main env: capital platforms, outposts, bandit camps) contain **no reference to any WP40/R5 module, IPC key, or payload** — their only shared surface with anything is `geometry.lua` via `grug_mapgen.geometry`, which is unrelated to `wp40/`. Combined with the prior pass's verification that no init file loads any R5 module and that `r5.lua` is reachable only by explicit tool `dofile`, the boundary is confirmed from both directions: the legacy writers are the only active world writers, and no configuration can enable both generations.

## 6. Foundation-zero ruling

**The zero production population of all three P2 `FOUNDATION_*` opcodes is a legitimate accepted-layout zero, not an evidence gap and not a defect.** The chain, now fully verified:

1. **Contract rule.** §8.3 mandates P2 `FOUNDATION_*` exactly when `hard_foundation` is true *and* the R3 functional kind is `anchor_platform`, and explicitly states that R5 does not turn arbitrary hard-protected columns into construction and that ingress corridors remain P4 (`r5-contract.md:1552-1569`). A non-hard platform takes P4 path opcodes per §8.4 (`:1573-1579`).
2. **Two independent implementations agree.** The planner implements the exact conjunction (`planner.lua:1295-1307`, with the non-hard platform falling into the P4 branch at `:1308-1321`), and the Validator's independently authored seed oracle implements it separately; both count zero over the complete seed-0 map scan, and the artifact's `mask/foundation = 0` and per-opcode zeros are those independently computed counts, cross-checked against materialized planner plan bytes at every witness group.
3. **Source-level derivation (new in this pass, from the fully read frozen inputs).** `anchor_platform` can arise only on a `planned_water` column inside the fitting square of a *selected* anchor (roster index ≥ 13; capitals are excluded in `fitting_grade_at`'s platform branch and start/capital grids are never consulted for it — `height.lua:1444-1473, 3287-3303`), owned by that anchor's own zone. `hard_foundation` is `hard_row_at(x, terrain_y, z) ~= nil` (`zones.lua:1413, 874-885`), membership in one of exactly 42 hard-protection footprints (`source/simple_map.lua:782-833`): six 128-wide start cores and six 532-wide capital aprons at anchors 1–12, six 128-wide ingress route corridors, and 24 single apex-socket columns. The intersection is empty on the V1e layout: none of the twelve start/capital zones (1, 3, 6, 8, 11, 13, 17, 19, 22, 24, 27, 29) owns *any* selected anchor (verified against all 100 anchor rows, `source/simple_map.lua:357-458`), and the fixed cores' columns are fixed-owned by those zones, so no selected fitting can satisfy the owner match there; the ingress corridors' planned-water columns are named causeway/bridge/ford operation columns or lie outside every same-zone selected fitting square (the nearest candidates, zone 34's clash fittings at (−2200,−80)/(−1768,64) with half-width 32, do not reach the route_043/049 corridor's intersection with the Gravesalt pans); and the apex sockets sit on islands that have no hydrology reaches at all, so no `planned_water` exists there. The y-conjunction is trivially robust (terrain ≥ authored floor −37 > −700 = every recipe's `y_min`).
4. **P2 is not untested code.** The adapter-side behavior of all three opcodes is exercised throughout the fixture evidence (policy apply matrix, dirty matrices, the 27-variant planner/adapter relation KAT, conflict corpus, owner-apply states).

Residual: unlike `TUNNEL_WALL` (which has a constructive §8.8 zero-proof), the foundation zero has no dedicated artifact proof key — but §14's proof set does not require one, so this is a documentation gap only. Recording the derivation above in R6/R7 planning is recommended before any layout change could make P2 live.

## 7. Residual limitations

1. **Hashes remain verified by cross-document consistency, not recomputation** — this pass is read-only with no interpreters, same as pass 1. The chain is mutually consistent everywhere compared.
2. **The seed oracle's candidate builder (validator lines 2624–3260) remains algorithmically parallel to the planner**; a shared misreading of the contract would evade their agreement. Mitigation is unchanged and now stronger: pass 1's line-by-line planner-versus-contract check plus this pass's independent §8.3/§8.4 and layout-level verification found no misreading, and the owner oracle's lighting/liquid model is genuinely independent.
3. **Legacy-writer semantics were reviewed for the R5 boundary only.** Their internal residual classes (documented extensively in their own headers, e.g. the four ocean-mask heal classes) are pre-R5 accepted behavior under WP36's records and were not re-adjudicated here.
4. Pass 1's briefing-hygiene note stands: any reuse of the Task C list should name `wp40-simple-map-v1e-refresh-contract.md` rather than the nonexistent `wp40-simple-map-r2-contract.md`.

## 8. Integration recommendation

**Merge R5 commit `212f822d` into the WP40 integration branch.** The complete-coverage bar that forced pass 1's NO-START is now met across the two passes combined: nothing in the previously unread validator regions, the legacy writers, or the eight frozen dependencies contradicts the accepted 2026-08-29 independent Opus review or the immutable artifact/evidence chain — every check made here corroborated them. Required actions, all documentation-level:

1. Disposition L1 and L2 as recorded residuals in the R6 contract or the durable R5 review record (no frozen R5 byte may be edited; no evidence regeneration is required).
2. Carry the Section 6 foundation-zero derivation into the R6/R7 planning record, so the first layout revision that could create a hard-protected `anchor_platform` column knowingly activates P2 rather than silently changing a zero.
3. Fix the Task C briefing's R2-contract filename if the list is reused.
