-- Alchemy v1 (Round 8): data-driven profession recipes on the brewing stand.

grug_alchemy = {}

local modpath = core.get_modpath(core.get_current_modname())
dofile(modpath .. "/effects.lua")
dofile(modpath .. "/recipes.lua")

