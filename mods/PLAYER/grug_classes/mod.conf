name = grug_classes
title = Grudgelands Classes
description = Class and race registry, character creation dialogs (race + class after faction), attribute/HP scaling via the level pipeline.
depends = grug_core, grug_factions, grug_xp
